{
  "name": "reactstopwatch",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "esbuild app.jsx --servedir=. --outdir=bundle --bundle  --sourcemap --define:process.env.NODE_ENV=\"'development'\"",
    "build": "esbuild app.jsx --outdir=bundle --bundle"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "dev-server": "^0.2.0",
    "esbuild": "^0.14.23",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
